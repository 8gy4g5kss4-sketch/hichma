/* ============================================
   HICHMA — APP INIT
   ============================================ */

document.addEventListener('DOMContentLoaded', () => {
  document.addEventListener('contextmenu', e => e.preventDefault());
  console.log('HICHMA — حشمة');
});